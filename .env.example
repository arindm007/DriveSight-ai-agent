# DriveSight Environment Configuration
# This file is generated by setup.sh - modify as needed

# ============================================================================
# GCP Configuration
# ============================================================================

# Your GCP Project ID (REQUIRED)
# Get this from: https://console.cloud.google.com/projectselector2/home/dashboard
GCP_PROJECT_ID=your-project-id

# GCS Bucket name for storing images
# Bucket is created by setup.sh
GCS_BUCKET=your-project-id-drivesight-images

# Firestore Collection name for storing analysis results
FIRESTORE_COLLECTION=analyses

# ============================================================================
# Model Configuration
# ============================================================================

# Gemini Model to use
# Options: gemini-2.0-flash-exp, gemini-2.0-flash, gemini-1.5-pro, etc.
GEMINI_MODEL=gemini-2.0-flash-exp

# Maximum image file size (in bytes)
# Default: 20MB (20971520 bytes)
MAX_IMAGE_SIZE=20971520

# Allowed image MIME types (comma-separated)
ALLOWED_FORMATS=image/jpeg,image/png,image/webp,image/gif

# ============================================================================
# API Configuration
# ============================================================================

# Request timeout in seconds
# Default: 60 seconds
REQUEST_TIMEOUT=60

# Cache TTL (Time To Live) in seconds
# Default: 3600 (1 hour)
# Set to 0 to disable caching
CACHE_TTL=3600

# ============================================================================
# Logging Configuration
# ============================================================================

# Log level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# Optional: Cloud Run Configuration
# ============================================================================

# Port for the API server
# Default: 8080
PORT=8080

# Number of workers for production (if using gunicorn)
# Default: 4
WORKERS=4

# ============================================================================
# Optional: Monitoring & Debugging
# ============================================================================

# Enable debug mode (more verbose logging)
# Default: false
DEBUG=false

# Enable request/response logging
# Default: false
LOG_REQUESTS=false

# ============================================================================
# Optional: Security
# ============================================================================

# CORS allowed origins (comma-separated)
# Default: * (all origins) - Change for production!
CORS_ORIGINS=*

# Enable authentication (future feature)
# Default: false
REQUIRE_AUTH=false

# ============================================================================
# Notes:
# ============================================================================
# 
# 1. Never commit actual secrets to version control
# 2. Use .gitignore to exclude this file in production
# 3. For Cloud Run, use Secret Manager instead of .env
# 4. GOOGLE_APPLICATION_CREDENTIALS should point to key.json (set by setup.sh)
# 5. Restart the application after changing any environment variables
#
# ============================================================================
